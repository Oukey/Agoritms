class BinarySearch:

    def __init__(self, array):
        self.array = array
        self.Left = 0
        self.Right = len(array) - 1

    def Step(self, N):
        step = len(self.array) // 2

    def GetResult(self):
        pass
